# adapter-sdk

defineAdapter, startAdapter, textResult and ToolGraftError. Registers native tools
with route-owned abort signals, validates inputs, bounds results, rechecks active
installation locally, and requests single-use confirmation for writes.
