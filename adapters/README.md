# Adapters

Build all five with `pnpm adapters:build`. Install each dist/adapter.tgz through
ToolGraft options. Sources and manifests are inspectable; builds default to local
review status. Playground has confirmed writes; HN, Wikipedia and the experimental
Reddit import are read only. See docs/authoring.md for the package contract.
