# api-engine

Fixed interpreter wrapper around pinned @webmcp-today/engine 0.1.0. Executes
validated upstream API definitions, preserves projection/transport behavior,
normalizes upstream failure text into typed isError results, and rejects dot paths.
