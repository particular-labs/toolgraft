# runtime-webmcp

Native document/navigator modelContext detection, collision handling, abort-owned
registration and the verified Chrome 153 client ABI. No polyfill or MAIN-world bridge.
See docs/browser-compatibility.md.
