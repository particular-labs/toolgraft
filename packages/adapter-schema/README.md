# adapter-schema

Shared manifest, tool, registry and revocation contracts; exact host matching;
input/output budgets; canonical hashing; bounded deterministic archive packing.
Browser-light runtime helpers live in src/runtime.ts to avoid schema code in script bundles.
